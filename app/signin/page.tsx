'use client'
import { useState } from 'react'
import { createClient } from '@/lib/supabase'
export default function SignIn(){const [email,setEmail]=useState('');const [code,setCode]=useState('');const [error,setError]=useState('');async function go(e:any){e.preventDefault();const r=await createClient().auth.signInWithPassword({email,password:code});if(r.error){setError('ورود ناموفق بود.')}else{location.href='/manage'}}return <main className="authPage"><div className="authCard"><a href="/" className="logo">Rahayi<span>رهایی</span></a><h1>ورود</h1><form onSubmit={go}><input aria-label="ایمیل" type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="ایمیل" required/><input aria-label="رمز" type="password" value={code} onChange={e=>setCode(e.target.value)} placeholder="رمز" required/>{error&&<p className="error">{error}</p>}<button className="cta" type="submit">ورود</button></form><a className="backLink" href="/signup">ساخت حساب مدیر</a></div></main>}
