import { notFound } from 'next/navigation'
import { articles as fallback } from '@/lib/articles'
import { serverDb } from '@/lib/db-server'
export const revalidate=30
export default async function ArticlePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const db=serverDb();const {data}=db?await db.from('articles').select('*').eq('slug',slug).eq('published',true).maybeSingle():{data:null};const article=data||fallback.find(x=>x.slug===slug);if(!article)notFound();const body=typeof article.body==='string'?article.body.split('\n').filter(Boolean):article.body;const date=article.published_at?new Date(article.published_at).toLocaleDateString('fa-IR'):article.date;return <main><header className="header"><div className="container nav"><a className="logo" href="/">Rahayi<span>رهایی</span></a><nav><a href="/#latest">آخرین مطالب</a><a href="/#about">درباره رهایی</a><a href="/manage">مدیریت</a></nav><a className="back" href="/">بازگشت به صفحه اصلی</a></div></header><article className="articlePage container"><div className="articleMeta"><span>{article.category}</span><span>{date}</span><span>{article.author}</span></div><h1>{article.title}</h1><p className="articleLead">{article.excerpt}</p><div className="articleBody">{body.map((p:string)=><p key={p}>{p}</p>)}</div><a className="cta" href="/">← بازگشت به مطالب</a></article><footer><div className="container"><strong>Rahayi</strong><span>رهایی؛ برای پرسیدن و فهمیدن.</span><small>© 2026 Rahayi</small></div></footer></main>}
