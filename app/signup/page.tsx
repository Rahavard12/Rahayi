'use client'
import {useState} from 'react'
import {createClient} from '@/lib/supabase'
export default function SignUp(){const [email,setEmail]=useState('');const [code,setCode]=useState('');const [msg,setMsg]=useState('');async function go(e:any){e.preventDefault();const {data,error}=await createClient().auth.signUp({email,password:code,options:{emailRedirectTo:'https://rahayi.vercel.app/manage'}});setMsg(error?'ثبت‌نام انجام نشد.':data.session?'حساب ساخته شد؛ وارد مدیریت می‌شوید.':'ایمیل تأیید برای شما ارسال شد. پس از تأیید، وارد شوید.')}return <main className="authPage"><div className="authCard"><a href="/" className="logo">Rahayi<span>رهایی</span></a><h1>ساخت حساب مدیر</h1><form onSubmit={go}><input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="ایمیل" required/><input type="password" value={code} onChange={e=>setCode(e.target.value)} placeholder="رمز عبور" minLength={8} required/><button className="cta" type="submit">ساخت حساب</button></form>{msg&&<p>{msg}</p>}<a className="backLink" href="/signin">قبلاً حساب دارید؟ ورود</a></div></main>}